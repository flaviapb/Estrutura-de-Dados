#include <stdio.h> //Biblioteca padrão que sempre precisa ser chamada, que significa: include = incluir e stdio = entrada e saída ou escrita e leitura.

int main(void){ //Função principal
    printf("Olá mundo\n"); // o "/n" serve para pular linha
    return 0;
}